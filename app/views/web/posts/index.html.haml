= stylesheet_link_tag :posts
- model_class = Post
.page-header
  %h1=t '.title'
- if signed_in?
  = link_to t('.new', :default => t("helpers.links.new")), new_post_path, :class => 'btn btn-primary'
  %br
  %br
%table.table.table-hover
  %tbody
    - @posts.each do |post|
      %tr{ onclick: "return location.href = '#{post_path(post)}'"}
        %td
          %span.label.label-success
            = post.human_story_state_name
        %td
          %img{ src: post.photo.thumb }
        %td= link_to post.id, post_path(post)
        %td= post.nickname
        %td= post.description.first 50
        %td= post.place.first 50
        %td= post.date
        %td=l post.created_at

- if signed_in?
  = link_to t('.new', :default => t("helpers.links.new")), new_post_path, :class => 'btn btn-primary'
- else
  = link_to "sign in", new_session_path
